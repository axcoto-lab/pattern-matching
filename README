# Run

Create a list of hostname, store in a file name `data` in current directory and
invoke it with rake:

```
rake
# Or to run on a different data source:
rake 'run[data/small.txt]'
```

To invoke with custom analyzer:

```
ANALYZER=NonDeliminator rake 'run[data/no-pattern.txt]'
```
